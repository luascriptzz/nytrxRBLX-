<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Script Hub</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', sans-serif; }
        body { background: #0d0d0d; color: #e0e0e0; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
        .card { background: #1a1a1a; padding: 2rem; border-radius: 12px; width: 90%; max-width: 400px; text-align: center; border: 1px solid #333; }
        h1 { font-size: 1.5rem; margin-bottom: 0.5rem; color: #fff; }
        p { color: #aaa; font-size: 0.9rem; margin-bottom: 1.5rem; }
        .status { display: inline-block; padding: 4px 10px; background: #16a34a; color: #fff; border-radius: 20px; font-size: 0.75rem; margin-bottom: 1rem; }
        .btn { display: block; width: 100%; padding: 12px; background: #2563eb; color: #fff; border: none; border-radius: 8px; font-size: 1rem; cursor: pointer; text-decoration: none; transition: 0.2s; }
        .btn:hover { background: #1d4ed8; }
        .links { margin-top: 1.5rem; font-size: 0.8rem; color: #666; }
        .links a { color: #888; text-decoration: none; margin: 0 5px; }
        .links a:hover { color: #fff; }
    </style>
</head>
<body>
    <div>
        <span>● ONLINE</span>
        <h1>[ YOUR SCRIPT NAME ]</h1>
        <p>Latest version ready. Free to use. Update logs in discord.</p>
        <a href="[ PUT UR DOWNLOAD/EXECUTE LINK HERE ]">GET SCRIPT</a>
        <div>
            <a href="#">Discord</a> • <a href="#">Support</a> • <a href="#">Status</a>
        </div>
    </div>
</body>
</html>
